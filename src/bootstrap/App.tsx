import React, { FC } from 'react';
import { ErrorBoundary } from '../components/ui-elements/common/ErrorBoundary';

const App:FC = () => {
  return (
    <ErrorBoundary>
      
    </ErrorBoundary>
  );
}

export default App;
